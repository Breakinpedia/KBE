server:
  port: 8080
spring:
  h2:
    console:
      enabled: true
  application:
    name: PRODUCT-SERVICE

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka/

  instance:
    hostname: localhost